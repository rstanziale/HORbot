package settings;

import org.apache.log4j.Logger;

/**
 * Define Logger interface for console messages
 *
 * @author Roberto B. Stanziale
 * @version 1.0
 */
public interface LoggerInterface {

    // LOGGER
    Logger logger = Logger.getLogger(LoggerInterface.class);

}
